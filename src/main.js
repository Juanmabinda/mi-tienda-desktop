// Splash mínimo. La ventana real arranca apuntada a https://mitiendapos.com.ar
// vía tauri.conf.json (`app.windows[0].url`), así que esto solo se ve si por
// algún motivo Tauri carga el frontendDist antes de navegar.
console.log("Mi Tienda POS desktop wrapper booted");
